# Client Setup

Install required frontend dependencies:

```bash
npm install @inertiajs/react @inertiajs/progress react react-dom tailwindcss @radix-ui/themes framer-motion gsap react-hook-form zod
```

Optional (only if still used for local UI state):

```bash
npm install zustand
```
